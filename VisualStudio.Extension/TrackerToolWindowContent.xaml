<DataTemplate xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
              xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
              xmlns:vs="http://schemas.microsoft.com/visualstudio/extensibility/2022/xaml"
              xmlns:img="clr-namespace:Microsoft.VisualStudio.Imaging;assembly=Microsoft.VisualStudio.Imaging"
              xmlns:catalog="clr-namespace:Microsoft.VisualStudio.Imaging;assembly=Microsoft.VisualStudio.ImageCatalog"
              xmlns:styles="clr-namespace:Microsoft.VisualStudio.Shell;assembly=Microsoft.VisualStudio.Shell.15.0"
              xmlns:colors="clr-namespace:Microsoft.VisualStudio.PlatformUI;assembly=Microsoft.VisualStudio.Shell.15.0">
    <Grid>
        <Grid.Resources>
            <!-- Color Resources -->
            <SolidColorBrush x:Key="YarnBlue" Color="#4A90E2"/>
            <SolidColorBrush x:Key="YarnPurple" Color="#8E44AD"/>
            <SolidColorBrush x:Key="YarnGreen" Color="#27AE60"/>
            <SolidColorBrush x:Key="YarnOrange" Color="#E67E22"/>
            <SolidColorBrush x:Key="YarnRed" Color="#E74C3C"/>
            <SolidColorBrush x:Key="SoftGray" Color="#ECF0F1"/>
            <SolidColorBrush x:Key="AccentBorder" Color="#3498DB"/>
            
            <!-- Label Styles -->
            <Style TargetType="Label" BasedOn="{StaticResource {x:Static styles:VsResourceKeys.ThemedDialogLabelStyleKey}}">
                <Setter Property="FontSize" Value="24" />
                <Setter Property="FontWeight" Value="SemiBold" />
                <Setter Property="Foreground" Value="{StaticResource YarnBlue}" />
            </Style>
            
            <!-- TextBox Styles -->
            <Style TargetType="TextBox" BasedOn="{StaticResource {x:Static styles:VsResourceKeys.TextBoxStyleKey}}">
                <Setter Property="FontSize" Value="24" />
                <Setter Property="TextAlignment" Value="Center" />
                <Setter Property="BorderBrush" Value="{StaticResource AccentBorder}" />
                <Setter Property="BorderThickness" Value="2" />
                <Setter Property="Padding" Value="8,5" />
                <Style.Triggers>
                    <Trigger Property="IsFocused" Value="True">
                        <Setter Property="BorderBrush" Value="{StaticResource YarnPurple}" />
                        <Setter Property="Background">
                            <Setter.Value>
                                <SolidColorBrush Color="#F8F9FA" Opacity="0.3"/>
                            </Setter.Value>
                        </Setter>
                    </Trigger>
                </Style.Triggers>
            </Style>
            
            <!-- Button Styles -->
            <Style TargetType="Button" BasedOn="{StaticResource {x:Static styles:VsResourceKeys.ButtonStyleKey}}">
                <Setter Property="FontSize" Value="24" />
                <Setter Property="FontWeight" Value="SemiBold" />
                <Setter Property="Padding" Value="12,6" />
                <Setter Property="Margin" Value="4" />
                <Setter Property="BorderThickness" Value="2" />
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Background" Value="{StaticResource YarnBlue}" />
                        <Setter Property="Foreground" Value="White" />
                        <Setter Property="BorderBrush" Value="{StaticResource YarnPurple}" />
                    </Trigger>
                </Style.Triggers>
            </Style>
            
            <!-- Special Button Styles -->
            <Style x:Key="NextButtonStyle" TargetType="Button" BasedOn="{StaticResource {x:Static styles:VsResourceKeys.ButtonStyleKey}}">
                <Setter Property="FontSize" Value="24" />
                <Setter Property="FontWeight" Value="Bold" />
                <Setter Property="Padding" Value="16,8" />
                <Setter Property="Margin" Value="4" />
                <Setter Property="Background" Value="{StaticResource YarnGreen}" />
                <Setter Property="Foreground" Value="White" />
                <Setter Property="BorderBrush" Value="{StaticResource YarnGreen}" />
                <Setter Property="BorderThickness" Value="2" />
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Background" Value="#229954" />
                        <Setter Property="BorderBrush" Value="#1E8449" />
                    </Trigger>
                </Style.Triggers>
            </Style>
            
            <Style x:Key="AddButtonStyle" TargetType="Button" BasedOn="{StaticResource {x:Static styles:VsResourceKeys.ButtonStyleKey}}">
                <Setter Property="FontSize" Value="24" />
                <Setter Property="FontWeight" Value="Bold" />
                <Setter Property="Padding" Value="16,8" />
                <Setter Property="Margin" Value="4" />
                <Setter Property="Background" Value="{StaticResource YarnOrange}" />
                <Setter Property="Foreground" Value="White" />
                <Setter Property="BorderBrush" Value="{StaticResource YarnOrange}" />
                <Setter Property="BorderThickness" Value="2" />
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Background" Value="#D35400" />
                        <Setter Property="BorderBrush" Value="#BA4A00" />
                    </Trigger>
                </Style.Triggers>
            </Style>
            
            <Style x:Key="IconButtonStyle" TargetType="Button" BasedOn="{StaticResource {x:Static styles:VsResourceKeys.ButtonStyleKey}}">
                <Setter Property="Width" Value="40" />
                <Setter Property="Height" Value="40" />
                <Setter Property="Margin" Value="2" />
                <Setter Property="BorderThickness" Value="2" />
                <Setter Property="BorderBrush" Value="{StaticResource YarnPurple}" />
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Background" Value="{StaticResource YarnPurple}" />
                        <Setter Property="BorderBrush" Value="#7D3C98" />
                    </Trigger>
                </Style.Triggers>
            </Style>
            
            <!-- TextBlock Styles -->
            <Style TargetType="TextBlock">
                <Setter Property="Foreground" Value="{StaticResource YarnBlue}" />
                <Setter Property="FontSize" Value="24" />
                <Setter Property="FontWeight" Value="Medium" />
                <Setter Property="TextAlignment" Value="Center" />
                <Setter Property="Margin" Value="5,10" />
            </Style>
        </Grid.Resources>
        
        <!-- Add a subtle background -->
        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1" Opacity="0.05">
                <GradientStop Color="#3498DB" Offset="0"/>
                <GradientStop Color="#8E44AD" Offset="1"/>
            </LinearGradientBrush>
        </Grid.Background>
        
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Instruction with colored background -->
        <Border Grid.ColumnSpan="4" Margin="5" Padding="15,10" 
                Background="{StaticResource SoftGray}" 
                BorderBrush="{StaticResource AccentBorder}" 
                BorderThickness="0,0,0,3"
                CornerRadius="5,5,0,0">
            <TextBlock Text="{Binding Instruction}" 
                       Foreground="{StaticResource YarnPurple}"
                       FontWeight="Bold" />
        </Border>

        <!-- Row -->
        <Label Content="Row:" Grid.Row="1" />
        <TextBox Text="{Binding Row}" Grid.Row="1" Grid.Column="1" />
        <Button Content="Next" 
                Command="{Binding NextRowCommand}" 
                IsEnabled="{Binding NextRowCommand.RunningCommandsCount.IsZero}" 
                Grid.Row="1" Grid.Column="2" Grid.ColumnSpan="2"
                Style="{StaticResource NextButtonStyle}" />

        <!-- Stitch -->
        <Label Content="Stitch:" Grid.Row="2" />
        <TextBox Text="{Binding Stitch}" Grid.Row="2" Grid.Column="1" />
        <Button Content="Add" 
                Command="{Binding AddStitchesCommand}" 
                CommandParameter="{Binding Increase}" 
                IsEnabled="{Binding AddStitchesCommand.RunningCommandsCount.IsZero}" 
                Grid.Row="2" Grid.Column="2" Grid.ColumnSpan="2"
                Style="{StaticResource AddButtonStyle}" />

        <!-- Increase -->
        <Label Content="Increase:" Grid.Row="3" />
        <TextBox Text="{Binding Increase}" Grid.Row="3" Grid.Column="1" />
        <Button Command="{Binding AddOrRemoveIncreaseCommand}" 
                CommandParameter="-" 
                IsEnabled="{Binding AddOrRemoveIncreaseCommand.RunningCommandsCount.IsZero}" 
                Grid.Row="3" Grid.Column="2"
                Style="{StaticResource IconButtonStyle}">
            <img:CrispImage Moniker="{x:Static catalog:KnownMonikers.Remove}" Width="24" Height="24" />
        </Button>
        <Button Command="{Binding AddOrRemoveIncreaseCommand}" 
                CommandParameter="+" 
                IsEnabled="{Binding AddOrRemoveIncreaseCommand.RunningCommandsCount.IsZero}" 
                Grid.Row="3" Grid.Column="3"
                Style="{StaticResource IconButtonStyle}">
            <img:CrispImage Moniker="{x:Static catalog:KnownMonikers.Add}" Width="24" Height="24" />
        </Button>
    </Grid>
</DataTemplate>
