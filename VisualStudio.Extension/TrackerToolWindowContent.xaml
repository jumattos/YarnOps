<DataTemplate xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
              xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
              xmlns:vs="http://schemas.microsoft.com/visualstudio/extensibility/2022/xaml"
              xmlns:img="clr-namespace:Microsoft.VisualStudio.Imaging;assembly=Microsoft.VisualStudio.Imaging"
              xmlns:catalog="clr-namespace:Microsoft.VisualStudio.Imaging;assembly=Microsoft.VisualStudio.ImageCatalog"
              xmlns:styles="clr-namespace:Microsoft.VisualStudio.Shell;assembly=Microsoft.VisualStudio.Shell.15.0"
              xmlns:colors="clr-namespace:Microsoft.VisualStudio.PlatformUI;assembly=Microsoft.VisualStudio.Shell.15.0">
    <Grid>
        <Grid.Resources>
            <Style TargetType="Label" BasedOn="{StaticResource {x:Static styles:VsResourceKeys.ThemedDialogLabelStyleKey}}" />
            <Style TargetType="TextBox" BasedOn="{StaticResource {x:Static styles:VsResourceKeys.TextBoxStyleKey}}" />
            <Style TargetType="Button" BasedOn="{StaticResource {x:Static styles:VsResourceKeys.ButtonStyleKey}}" />
            <Style TargetType="TextBlock">
                <Setter Property="Foreground" Value="{DynamicResource {x:Static styles:VsBrushes.WindowTextKey}}" />
            </Style>
        </Grid.Resources>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <!-- Instruction -->
        <TextBlock Text="{Binding Instruction}" Grid.ColumnSpan="4" />

        <!-- Row -->
        <Label Content="Row:" Grid.Row="1" />
        <TextBox Text="{Binding Row}" Grid.Row="1" Grid.Column="1" />
        <Button Content="Next" Command="{Binding NextRowCommand}" IsEnabled="{Binding NextRowCommand.RunningCommandsCount.IsZero}" Grid.Row="1" Grid.Column="2" Grid.ColumnSpan="2" />

        <!-- Stitch -->
        <Label Content="Stitch:" Grid.Row="2" />
        <TextBox Text="{Binding Stitch}" Grid.Row="2" Grid.Column="1" />
        <Button Content="Add" Command="{Binding AddStitchesCommand}" CommandParameter="{Binding Increase}" IsEnabled="{Binding AddStitchesCommand.RunningCommandsCount.IsZero}" Grid.Row="2" Grid.Column="2" Grid.ColumnSpan="2" />

        <!-- Increase -->
        <Label Content="Increase:" Grid.Row="3" />
        <TextBox Text="{Binding Increase}" Grid.Row="3" Grid.Column="1" />
        <Button Command="{Binding AddOrRemoveIncreaseCommand}" CommandParameter="-" IsEnabled="{Binding AddOrRemoveIncreaseCommand.RunningCommandsCount.IsZero}" Grid.Row="3" Grid.Column="2">
            <img:CrispImage Moniker="{x:Static catalog:KnownMonikers.Remove}" Width="16" Height="16" />
        </Button>
        <Button Command="{Binding AddOrRemoveIncreaseCommand}" CommandParameter="+" IsEnabled="{Binding AddOrRemoveIncreaseCommand.RunningCommandsCount.IsZero}" Grid.Row="3" Grid.Column="3">
            <img:CrispImage Moniker="{x:Static catalog:KnownMonikers.Add}" Width="16" Height="16" />
        </Button>
    </Grid>
</DataTemplate>
